{
  "ext": "ts",
  "execMap": {
    "ts": "node --inspect -r esm -r dotenv/config -r tsconfig-paths/register -r ts-node/register/type-check"
  },
  "ignore": [
    "**/test.*",
    "**/*.test.*"
  ],
  "watch": [
    "src/**/*.ts"
  ]
}
